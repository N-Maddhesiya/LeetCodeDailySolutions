class Solution {
public:
    int findMin(vector<int>& nums) {
        int a;
        while(1){
            if(is_sorted(nums.begin(),nums.end())){
                a=nums[0];
                break;
            }
            rotate(nums.begin(),nums.begin()+1,nums.end());
        }
        return a;
    }
};
